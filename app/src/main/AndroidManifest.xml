<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dsccvrgubeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dsc_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dsc_logo"
        android:supportsRtl="true"
        android:theme="@style/AppMaterialTheme">
        <activity android:name=".views.LoginPage">

        </activity>
        <activity android:name=".views.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".views.SignupPage">

        </activity>
        <activity android:name=".views.OnBoardingPage"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>